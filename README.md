# IMAGE-GALLERY
This project is a responsive image gallery built using HTML and CSS. Images are displayed in grayscale by default and smoothly transition to full color on hover using CSS filter and transition. The gallery also uses rounded cards, hover effects, and clean layout styling to create a modern visual experience.
